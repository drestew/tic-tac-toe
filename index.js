const gameBoard = (() => {
    const board = ['topL, topC, topR, midL, midC, midR, botL, botC, botR']
    return { board }
})()

const displayController = (() => {
    const user = "x"
    const computer = "o"
})